info:
  name: Meeting Get One
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/meetings/:id
  params:
    - name: id
      value: "8"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
