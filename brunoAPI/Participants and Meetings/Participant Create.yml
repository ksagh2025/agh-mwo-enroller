info:
  name: Participant Create
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/participants
  body:
    type: json
    data: |-
      {
        "login": "testowy2219",
        "password": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
