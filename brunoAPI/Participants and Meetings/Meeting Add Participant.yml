info:
  name: Meeting Add Participant
  type: http
  seq: 19

http:
  method: POST
  url: http://localhost:8080/meetings/:id/participants
  params:
    - name: id
      value: "7"
      type: path
  body:
    type: json
    data: |
      {
      "login" : "testowy2219"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
