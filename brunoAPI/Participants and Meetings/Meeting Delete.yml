info:
  name: Meeting Delete
  type: http
  seq: 16

http:
  method: DELETE
  url: http://localhost:8080/meetings/:id
  params:
    - name: id
      value: "8"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
