info:
  name: Participant Modify
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/participants/:id
  params:
    - name: id
      value: testowy2219
      type: path
  body:
    type: json
    data: |-
      {
        "login": "test20250513",
        "password": "testtest"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
