info:
  name: Meeting Get Participants
  type: http
  seq: 23

http:
  method: GET
  url: http://localhost:8080/meetings/:id/participants
  params:
    - name: id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
